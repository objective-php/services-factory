setup:
    composer:
        action: "install"

test:
    php_unit:
        config:
            - "phpunit.xml"
        directory:
            - "tests/"
        run_from: "tests/"
        coverage: "tests/coverage"
    php_mess_detector:
        allow_failures: true
    
